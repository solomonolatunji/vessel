{
  "name": "@codedock/docs",
  "type": "module",
  "version": "0.1.0",
  "description": "Codedock documentation site — Astro 7 + Starlight.",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev --port 4322",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist/ --project-name codedock-docs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.3",
    "astro": "^7.1.1"
  }
}
